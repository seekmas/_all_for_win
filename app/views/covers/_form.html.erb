<%= simple_form_for(@cover) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :cover %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
